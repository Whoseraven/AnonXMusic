user:
  name: Whosekirito
  github: Whosekirito

language: "python"
python_version: "3.10"

commands:
  build:
    - "pip install --upgrade pip"
    - "pip install -r requirements.txt"

  package:
    - ""  # No packaging needed for Python runtime apps

  start:
    - "python3 AnonXMusic"
